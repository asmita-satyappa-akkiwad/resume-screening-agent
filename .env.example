# Copy this file to ".env" and fill in your key.
# The app runs fine WITHOUT this key too — it just falls back to
# template-based reasoning instead of LLM-generated reasoning.
ANTHROPIC_API_KEY=your_api_key_here
